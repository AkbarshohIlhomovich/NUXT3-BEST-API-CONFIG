{
  "name": "nuxt-app",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "generate:api": "npx openapi-typescript https://api-oshxona.ikramov.uz/swagger/?format=openapi -o api/endpoints.ts"
  },
  "devDependencies": {
    "@nuxt/devtools": "latest",
    "@types/node": "^18",
    "nuxt": "^3.6.2",
    "openapi-typescript": "^5.4.2"
  }
}
